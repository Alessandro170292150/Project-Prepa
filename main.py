import numpy as np
import time as t

def sum (t1, t2):
    n = np.size(t1)
    t3 = np.zero(n)
    


def strassen (t1, t2):
    n = np.size(t1)
    c = np.zero(n)
    assert (np.size(t2) != n)
    a1 = t1.reshape()
    a2 = t1.reshape()
    a3 = t1.reshape()
    a4 = t1.reshape()
    b1 = t2.reshape()
    b2 = t2.reshape()
    b3 = t2.reshape()
    b4 = t2.reshape()
    m1 = (a1 + a4) * (b1 + b4)
    m2 = 
    m3 = 
    m4 = 
    m5 = 
    m6 = 
    m7 = 
    for i in range(n//2):
        for j in range(n//2):
            a1 []